<nav class="navbar fixed-top bg-white navbar-expand-lg scrolling-navbar double-nav">

    <!-- SideNav slide-out button -->
    <div class="float-left">
        <a href="#" data-activates="slide-out" class="button-collapse"><i class="fas fa-bars"></i></a>
    </div>

    <!-- Breadcrumb -->
    <div class="breadcrumb-dn mr-auto font-weight-bold">
        <p>Tableau de bord</p>
    </div>

    <div class="d-flex change-mode">

        <!-- Navbar links -->
        <ul class="nav navbar-nav nav-flex-icons ml-auto">

            <!-- Dropdown -->
            <li class="nav-item">
                <a class="nav-link waves-effect" id="navbarDropdownMenuLink"
                   aria-haspopup="true" aria-expanded="false" style="color: black!important;">
                    <i class="far fa-bell"></i>
                    <sup class="badge rounded-circle" style="background-color: #ff5252!important">{{ (commandToDay is defined )? commandToDay|length : 0 }}</sup>
                </a>
            </li>

            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle waves-effect font-weight-bold" href="#" id="userDropdown" data-toggle="dropdown"
                   aria-haspopup="true" aria-expanded="false" style="color: #424242!important;">
                    <i class="fas fa-user"></i> <span class="clearfix d-none d-sm-inline-block">Profile</span>
                </a>
                <div class="dropdown-menu dropdown-menu-right" aria-labelledby="userDropdown">
                    <a class="dropdown-item" href="https://sapeurdebaby.com" target="_blank" style="color: #424242!important">
                        <i class="fab fa-internet-explorer mr-2"></i>Accéder à la page d'accueil</a>
                    <a class="dropdown-item" href="{{ path('logout') }}" style="color: #424242!important">
                        <i class="fas fa-sign-out-alt mr-2"></i>Se déconnecter</a>
                </div>
            </li>

        </ul>
        <!-- Navbar links -->

    </div>

</nav>